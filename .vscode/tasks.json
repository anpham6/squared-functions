{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gitCommitAmendNoEdit",
            "type": "shell",
            "command": "git commit --amend --no-edit && git push --force-with-lease origin master"
        }
    ]
}